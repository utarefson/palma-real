import { PagePlaceholder } from '../components/PagePlaceholder';

export function ParkingPage() {
  return (
    <PagePlaceholder
      title="Parking"
      description="Manage parking spaces and tenant parking assignments."
    />
  );
}
